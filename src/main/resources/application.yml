
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/us?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC
    username: root
    password: 123
    driver-class-name: com.mysql.jdbc.Driver
  thymeleaf:
    cache: false      #开发阶段关闭缓存

mybatis:
  mapper-locations: classpath:/mapper/*.xml
server:
  port: 8080
